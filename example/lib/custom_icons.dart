import 'package:flutter/widgets.dart';

/// The IconData of our focus for this example is
///
///     CustomIcons.logo_bugbash
/// and it is not a square glyph, so a regular [Icon] will present it only
/// *mostly* correctly and will not provide proper horizontal space for it.
class CustomIcons {
  /// The IconData of our focus for this example is
  ///
  ///     CustomIcons.logo_bugbash
  /// and it is not a square glyph, so a regular [Icon] will present it only
  /// *mostly* correctly and will not provide proper horizontal space for it.
  static const IconData logo_bugbash =
      IconData(0x007E, fontFamily: 'BashinBugs-Stripped');
}
